*IMPORTANT*: Use `ask_user_question` to clarify requirements and decisions whenever possible; batch related questions, use `multiSelect` when needed, and use `preview` options with code snippets to illustrate choices.

- Avoid writing comments in code; prefer clear, concise names.
- Apply Clean Code, SOLID, and DRY principles.
- Use `todo` for non-trivial or multi-step tasks to track progress clearly.
- Use subagents for smaller independent tasks to save context/tokens, and verify their results before relying on them.
- Use web-access tools when external docs, up-to-date information, or examples outside the repo are needed.
